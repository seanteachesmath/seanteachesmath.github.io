<?xml version="1.0" encoding="UTF-8" ?>
<section xml:id="derivatives-of-exponential-and-logarithmic-functions">
<title>Derivatives of exponential and logarithmic
functions</title>


<paragraphs xml:id="the-plan-10">
<title>The Plan</title>

<p> Given the rather intense computations required to
compute derivatives of polynomials, we will take a different approach to
study derivatives of exponential and logarithmic functions.
</p>


<paragraphs xml:id="derivative-of-ex">
<title>Derivative of <m>e^x</m></title>

<p>	The function <m>f(x)=e^x</m> is an exponential
function <term>not</term> a power function. So far we have no rules to
compute the derivative, only a calculation using the limit definition of
the derivative.
</p>

<p>	What does <m>f(x)=e^x</m> look like?
</p>

<p>	How does it behave?
</p>

<p>	What is its derivative?
</p>

<figure>
<image xml:id="image-graph-derivative-exponential"  source="graph.png" width = "50%"/>
<caption>image</caption>
</figure>


<paragraphs xml:id="fx-from-the-definition">
<title><m>f'(x)</m> from the definition</title>

<p> We proceed using the limit definition.
</p>

</paragraphs>

<paragraphs xml:id="surprising-facts-about-ex">
<title>Surprising facts about <m>e^x</m></title>

<p> A few surprises emerge when dealing with
<m>\displaystyle \frac{d}{dx}\Big( e^x\Big).</m>
</p>

</paragraphs>

<paragraphs
xml:id="derivative-of-the-natural-logarithm-function-lnx">
<title>Derivative of the natural logarithm function
<m>\ln(x)</m></title>

<p> We state without proof or calculation a few
facts about the derivative of the natural logarithm function.
</p>

<p> What does <m>f(x)=\ln(x)</m> look like?
</p>

<p> How does it behave?
</p>

<p> What is its derivative?
</p>

<figure>
<image xml:id="image-graph-derivative-logarithm"  source="graph.png" width = "50%"/>
<caption>image</caption>
</figure>

</paragraphs>

<paragraphs xml:id="consequences">
<title>Consequences</title>

<p> We are now able to compute derivatives of a
variety of functions that involve exponential or logarithmic terms.
First we list a few special cases
</p>

<p><ul>
<li> <p> <m>\displaystyle \frac{d}{dx}\Big(Ke^x\Big)
=</m></p> </li>
<li> <p> <m>\displaystyle \frac{d}{dx}\Big(e^{cx}\Big)
=</m></p> </li>
<li> <p> <m>\displaystyle \frac{d}{dx}\Big(\ln(Kx)\Big)
=</m></p> </li>
<li> <p> <m>\displaystyle \frac{d}{dx}\Big(\ln(x^c)\Big)
=</m></p> </li>

</ul></p>

</paragraphs>
</paragraphs>
</paragraphs>

<exercises xml:id="quick-practice-exp-deriv" class="unnumbered">
<title>Quick practice</title>


<exercise><statement> <p> What is the derivative of <m>f(x) =
3e^x</m>?</p> </statement>
<solution>
<p> The derivative of <m>f(x) = 3e^x</m> is <m>f'(x) =
\dfrac{d}{dx}\left( 3e^x\right) = 3\dfrac{d}{dx}\left(e^x\right) =
3e^x</m>.</p>
</solution>
</exercise>

<exercise><statement> <p> What is the derivative of <m>f(x) =
3e^{7x}</m>?</p> </statement>
<solution><p> The derivative of <m>f(x) = 3e^x</m> is <m>f'(x) =
\dfrac{d}{dx}\left( 3e^x\right) = 3\dfrac{d}{dx}\left(e^x\right) =
3e^x</m>.</p>
</solution>
</exercise>

<exercise><statement> <p> What is the second derivative of <m>f(x) =
\ln(x)</m>?</p> </statement>
<solution><p> To find the second derivative of <m>f(x) = \ln(x)</m>,
we need the first derivative <m>f'(x) = \dfrac{1}{x}</m>. The first
derivative is <m>f'(x) = \dfrac{1}{x} = x^{-1}</m> (recall the rules for
exponents/powers). The derivative of <m>f'(x)</m> or second derivative
of <m>f(x)</m> is, <me>f''(x) = \dfrac{d}{dx}\left(x^{-1}\right) =
-1(x^{-2}) = -x^{-2}</me> (bring the old power down and reduce by one
for the new power).</p>
</solution>
</exercise>

</exercises>
</section>

<!-- ******************************* -->
